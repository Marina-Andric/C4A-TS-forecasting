import numpy as np

a = [1, 1, 1, 1, 1, 1]
b = [0, 2, 0, 2, 0, 2]

print ('list a: avg = %.2f and std = %.2f' % (np.mean(a), np.std(a)))
print ('list b: avg = %.2f and std = %.2f' % (np.mean(b), np.std(b)))
